# Red Lines — Абсолютные запреты

Эти правила не обсуждаются и не имеют исключений.
Если кажется что есть веская причина нарушить — СТОП. Спроси у пользователя явно.

---

## НЕЛЬЗЯ без явного «да, удали» от пользователя
**Удалять файлы.** Переименовать, закомментировать, переместить — можно.
Удалить — только если пользователь написал именно «удали».

> История: AI интерпретировал «убери это» как удаление файла. Данные потеряны.

---

## НЕЛЬЗЯ рефакторить рабочий код «заодно»
Если задача — добавить фичу, занимайся только ей.
Увидел некрасивый код рядом — скажи об этом, но не трогай.

> История: «Чуть причесал попутно» → сломал логику, которая была намеренно такой.

---

## НЕЛЬЗЯ менять публичный API без обсуждения
Публичный API = любая функция в namespace KTools, любой event, любой SavedVariables ключ.
Если нужно изменить — предложи, объясни почему, жди ответа.

> История: Сломали сохранение позиции ширины окна.

---

## НЕЛЬЗЯ считать «странные» значения багом
Если в коде что-то выглядит странно (== 1 вместо "WEAPONS", магические числа, хардкод) —
сначала СПРОСИ почему так. Не «исправляй» молча.

> История: AI «исправил» намеренный workaround → часовой дебаг.

---

## НЕЛЬЗЯ добавлять зависимости без обсуждения
KTools не тянет сторонние библиотеки без явного решения.
Увидел что можно использовать LibX — предложи, объясни trade-off, жди ответа.

> История: Реализация собственной обработки SavedVariables вместо подключения бибилотеки - содержала критический баг сломавший клиент.
---

## НЕЛЬЗЯ коммитить в main напрямую
Любые изменения — срого через .patch файл.

> Доступ к репозиторию только на чтение.

---

## НЕЛЬЗЯ писать код «на глаз»
Legion, Retail, Classic Era и Cata — разные API. Перед тем как использовать функцию —
убедись что она существует в целевой версии. Если не уверен — скажи об этом.

> История: Поднятие предмета пыталось вызвать функцию, которая в Retail была переобозначена на Logoout.
