-- .claude/CLAUDE.md
Проект: WoW addon (Legion 7.3.5), Lua
Репо: github.com/kawfia/KToolsCore

---

## Модель общения

АИ и пользователь не имеют прямого доступа к локальным репозиториям друг друга.
Единственная точка синхронизации — удалённый репозиторий.

- Пользователь вносит правки локально → коммит → пуш в удалённый репо.
- АИ вытягивает удалённый репо → генерирует код в нужном формате → пользователь применяет локально.
- АИ **не может пушить**. Репо для АИ — только на чтение.

---

## Цикл работы

### Каждое сообщение (первое или последующее в сессии):

1. **Синхронизация** — хук автоматически вытягивает удалённый репо, перезаписывая локальное.
2. **Загрузка документации** — обязательно прочитать:
   - `.claude/rules/red-lines.md`
   - `.claude/REFERENCE.md`
   - `.claude/tasks/<модуль>.md` — если задача касается конкретного модуля
3. **Проверка реализуемости** — убедиться что задача не нарушает ограничения.
   Если документация не даёт ответа — **остановиться, задать вопрос пользователю**.
4. **Агент-советник** — запустить Agent, проверить reference на похожие реализации.
   Если агент вернул `REF:` — остановиться, сообщить пользователю, ждать его решения.
5. **Генерация кода** — текстовые блоки с указанием пути к файлу (см. ниже).
6. **Слепой ревью** — запустить Agent и передать ему код + правила (без задачи пользователя).
7. **Финал** — сообщить пользователю что обновить в `.claude/MEMORIES.md`.

После этого: пользователь применяет код локально → коммит → пуш → следующий цикл.

---

## Генерация кода

### Способ 1 — полный файл (на выбор AI от сложности правок)
Новый файл **или** более двух правок на файл:
- вывести файл целиком в блоке кода
- первой строкой — путь к файлу (начиная с папки аддона)

### Способ 2 — замена фрагмента (на выбор AI от сложности правок)
До 5 строк, не более двух правок на файл:
- первый блок — старый код (с путём к файлу)
- второй блок — новый код (с путём к файлу)

### Способ 3 - создание патча (строго по запросу пользователя).
Файл генерируется и отправляется через чат пользователю.
- Описание коммита: тэги [task-xxx] [bug-xxx] [ref-xxx]
- Себя указывать как контрибьютера.

---

## Агент-советник

Запускается **до генерации кода**. Цель — проверить, можно ли решить задачу с помощью существующего reference вместо написания нового кода.

Вызвать Agent с model: "haiku"
- [ ] краткую формулировку: **что** нужно реализовать (этап задачи с комментариями пользователя)
- [ ]**передать**:
- текущий код затронутых файлов
- `.claude/REFERENCE.md`
- релевантное содержимое `.claude/reference/` (референсные аддоны, диаграммы, core.h)

Ожидаемый ответ агента:
- `OK` — похожих реализаций не найдено, генерация кода может начинаться.
- `REF: <описание>` — найдена похожая реализация или паттерн. Сообщить пользователю, остановиться до его решения.

---

## Слепой ревью

Вызвать Agent с model: "sonnet"
- [ ] **без описания задачи пользователя**
- [ ] передать:
- сгенерированный код
- `.claude/rules/red-lines.md`
- `.claude/rules/architecture.md`
- `.claude/tasks/<модуль>.md` — если код касается модуля
- `.claude/REFERENCE.md`

Ожидаемый ответ агента: `OK` или список нарушений.

---

## Финал ответа

Сообщить пользователю что добавить и что удалить из `.claude/MEMORIES.md` оформленным блоком кода.
Скользящее окно: **5-7 событий**.